# Warp API 认证配置
WARP_JWT=your_jwt_token_here
WARP_REFRESH_TOKEN=your_refresh_token_here

# 本地JWT文件路径配置（可选）
# 如果设置了此路径，系统会从指定的JSON文件中读取refresh_token进行刷新
# 文件格式应为包含refresh_token字段的JSON数组
# LOCAL_JWT_FILEPATH=tmp/warp_accounts_simple.json

# 服务器端口配置
WARP_SERVER_PORT=8000
OPENAI_SERVER_PORT=8010

# 服务器主机配置
HOST=127.0.0.1

# 桥接服务器URL配置（可选，会自动根据WARP_SERVER_PORT生成）
# WARP_BRIDGE_URL=http://localhost:8000

# 日志配置
WARP_LOG_LEVEL=info
WARP_ACCESS_LOG=true
OPENAI_LOG_LEVEL=info
OPENAI_ACCESS_LOG=true

# OpenAI兼容层配置
WARP_COMPAT_INIT_RETRIES=10
WARP_COMPAT_INIT_DELAY=0.5
WARP_COMPAT_WARMUP_RETRIES=3
WARP_COMPAT_WARMUP_DELAY=1.5

# System Prompt 配置
# 服务器级别的系统提示词，会在每次对话最前端注入（可选）
# 注意:长提示词、多行内容、包含特殊字符的情况，必须使用引号括起来
# SYSTEM_PROMPT=你是一个专业的AI助手，请遵循相关规范。

# System Prompt 处理模式
# merge（默认）：环境变量优先，然后合并客户端的 system 消息，自动去重
# replace：完全使用环境变量的 SYSTEM_PROMPT，忽略客户端的所有 system 消息
# SYSTEM_PROMPT_MODE=merge

# 配额刷新阈值配置
# 当剩余配额少于此值时，自动刷新匿名账户（设置为0禁用配额检查）
WARP_QUOTA_REFRESH_THRESHOLD=0
